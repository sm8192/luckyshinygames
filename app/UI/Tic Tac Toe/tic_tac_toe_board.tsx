'use client'

import {useState} from "react";

interface ticTacToeBoardProps {
    game: string,
    boardId: number
}

export default function TicTacToeBoard({ game, boardId }: ticTacToeBoardProps) {
    return (
        <div>BoardGoesHere</div>
    )
}